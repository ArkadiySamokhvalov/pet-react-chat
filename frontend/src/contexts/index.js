import { createContext } from 'react';

const AuthContext = createContext({});
const SocketContext = createContext();
const ModalContext = createContext();

export { AuthContext, SocketContext, ModalContext };
